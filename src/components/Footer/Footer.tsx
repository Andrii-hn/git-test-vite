import classes from "./Footer.module.scss"

const Footer = () => {
    return (
        <p className={classes.pageBottom}>Click something and you'll like it i guess.</p>
    )
}

export default Footer