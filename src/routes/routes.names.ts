export enum COMMON_ROUTES { 
    HOME = "/",
    LOGIN = "/login",
    REGISTER = "/register",
    PRODUCTS = "/products",
    NOT_FOUND = "/404"
}

export enum USER_ROUTES {
    CART = "/cart",
    ORDERS_HISTORY = "/history"
}

export enum SELLER_ROUTES {
    DASHBOARD = "/dashboard",
    ORDERS = "/orders",
    ANALYTICS = "/insights"
} 